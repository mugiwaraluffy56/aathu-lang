version: '3.8'
services:
  registry:
    build: { context: ../../web/registry }
    ports: ["3000:3000"]
    depends_on: [db]
  db:
    image: postgres:16
    environment: { POSTGRES_DB: aathu, POSTGRES_PASSWORD: postgres }
