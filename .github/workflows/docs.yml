name: Deploy Docs
on: { push: { branches: [main], paths: ['web/docs/**'] } }
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: cd web/docs && npm install && npm run build
