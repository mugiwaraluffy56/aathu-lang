// Simple HTTP server
import http

fn main() {
    let server = http.Server()

    server.get("/", fn(req) {
        return { status: 200, body: "Hello from aathu!" }
    })

    server.listen(8080)
    print("Server running on :8080")
}
